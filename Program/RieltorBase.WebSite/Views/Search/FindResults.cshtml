@using RieltorBase.Domain.InfoClasses;
@model IEnumerable<RealtyObjectInfo>

@{
    ViewBag.Title = "Find";
}

<h2>Find results</h2>

<table class="table table-bordered">
    <thead>
        <tr>
            <td>Id</td>
            <td>Адрес</td>
            <td>Цена</td>
        </tr>
    </thead>

    @foreach(RealtyObjectInfo info in this.Model)
    {
        <tr>
            <td>@info.Id</td>
            <td>@(info.Properties.ContainsKey("Адрес") ? info.Properties["Адрес"] : "")</td>
            <td>@(info.Properties.ContainsKey("Цена") ? info.Properties["Цена"] : "")</td>
            <td>@(info.Properties.ContainsKey("Address") ? info.Properties["Address"] : "")</td>
        </tr>
    }
</table>
